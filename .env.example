# Copy to ".env" and adjust as needed: `cp .env.example .env`
# docker compose reads .env automatically. All values have working defaults
# baked into docker-compose.yml, so .env is OPTIONAL for local development.

# ---- Database (MySQL container) ----
MYSQL_ROOT_PASSWORD=rootpw_dev
DB_NAME=fmsv_db
DB_USER=reporting_ro
DB_PASSWORD=txreport_dev_pw
DB_PORT=3306

# ---- Application ----
APP_PORT=8080
